<p>
    <strong> <%= comment.author.username %> </strong> said <%= comment.body %>
  </p>
  
  <p>
    <% if comment.author == @current_author %>
      <%= link_to "Destroy Comment", [comment.post, comment], data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
    <% end %>
  </p>